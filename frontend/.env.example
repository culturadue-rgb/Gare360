# Copia questo file in .env per lo sviluppo locale.
# Su Vercel questo valore si imposta in Settings -> Environment Variables.

# Indirizzo del backend su Render, senza barra finale.
# In sviluppo lascialo vuoto: Vite gira le richieste al backend locale.
VITE_API_URL=
